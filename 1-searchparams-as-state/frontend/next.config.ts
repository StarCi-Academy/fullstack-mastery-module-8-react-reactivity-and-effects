import type { NextConfig } from "next"

/**
 * Cấu hình Next.js — App Router mặc định.
 * (EN: Next.js config — default App Router.)
 */
const nextConfig: NextConfig = {
    reactStrictMode: true,
}

export default nextConfig
