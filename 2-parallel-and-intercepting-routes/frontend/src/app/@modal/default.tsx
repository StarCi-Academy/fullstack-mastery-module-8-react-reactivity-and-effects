/**
 * Default cho slot @modal — render null khi không có intercepting route match.
 * (EN: Default for the @modal slot — render null when no intercepting route matches.)
 */
export default function ModalDefault(): null {
    return null
}
